-- This defines diagnostic icons for use in Neovim.
-- See `03-mini-icons.lua` also.

return {
  diagnostic = {
    error = { glyph = ' ' },
    warn = { glyph = ' ' },
    info = { glyph = ' ' },
    hint = { glyph = '󰌵 ' },
  },
}
